mkdir ~/.ssh
echo -e $SSH_KEY>.ssh/gateway
cat >.ssh/config <<-EOF
host $GATEWAY
  User $SSH_USER
  IdentityFile ~/.ssh/gateway
  IdentitiesOnly yes
  StrictHostKeyChecking No
EOF
chmod 0400 .ssh/gateway

